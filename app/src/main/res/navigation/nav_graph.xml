<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/nav_host_fragment0">

    <fragment
        android:id="@+id/nav_host_fragment0"
        android:name="com.example.uitest.MainActivity"
        android:label="layout"
        tools:layout="@layout/layout" >
        <action
            android:id="@+id/action1"
            app:destination="@id/nav_host_fragment1" />
    </fragment>

    <fragment
        android:id="@+id/nav_host_fragment1"
        android:name="com.example.uitest.newActivity2"
        android:label="constraintlayout"
        tools:layout="@layout/constraintlayout" />
</navigation>